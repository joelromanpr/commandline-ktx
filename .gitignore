# ----------------------------------------------------------------------
# 1. BUILD SYSTEM AND OUTPUT
# ----------------------------------------------------------------------
# Gradle build directory (contains all generated files, classes, etc.)
/build
/out

/demo/build
/library/build

# Gradle wrapper caches and files
.gradle

# Kotlin Gradle plugin data (as you correctly included)
.kotlin/

# Standard Java/JVM compiled output
*.class
*.war
*.ear
*.zip
*.tar.gz
*.rar
*.nar

# ----------------------------------------------------------------------
# 2. SECRETS AND CREDENTIALS 🔒
# ----------------------------------------------------------------------
# Common files used for API keys, passwords, and private configuration.
# If you use a special file name for secrets, add it here.
local.properties
/secrets/
.env
.apikey
*.key
*.pem
*.p12

# Gradle properties files used to store credentials locally
# gradle.properties
# Note: Keep the main gradle.properties in source control if it only contains build config,
# but ensure local changes with credentials are NOT committed. It's safer to use local.properties.

# ----------------------------------------------------------------------
# 3. IDE-SPECIFIC FILES (IntelliJ IDEA)
# ----------------------------------------------------------------------
# IntelliJ IDEA project files (metadata, workspace)
.idea
*.iml
*.iws
*.ipr

# ----------------------------------------------------------------------
# 4. OPERATING SYSTEM & MISC
# ----------------------------------------------------------------------
# Log and temporary files
*.log
*.bak
*.swp

# Virtual machine crash logs
hs_err_pid*
replay_pid*

# Mac OS X
.DS_Store

# Windows
Thumbs.db